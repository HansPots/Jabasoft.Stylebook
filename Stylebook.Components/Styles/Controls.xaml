<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        Gedeelde control-stijlen die elke app via Theme.xaml binnenkrijgt.
        Impliciet (geen x:Key) zodat een kale <ComboBox/> ergens in een app
        deze automatisch krijgt - net als de oude jabasoft-theme.css alle
        <select>-elementen stijlde zonder dat een pagina daar iets voor
        hoefde te doen.

        ComboBox specifiek: WPF's standaard ComboBoxTemplate negeert een
        simpele Background-property-setter voor het gesloten vak (de
        chrome komt uit de systeem-ControlTemplate) - vandaar een eigen
        Template hier, in plaats van alleen Setters.

        Merget Typography.xaml zelf, ook al doet Theme.xaml dat ook: een
        los ResourceDictionary-bestand kan StaticResource-sleutels van een
        ANDER bestand alleen vinden als het ze zelf meeneemt (in
        tegenstelling tot een UserControl, dat via de hele elementenboom
        zoekt) - zonder dit gaf elke {StaticResource ...} hieronder
        DependencyProperty.UnsetValue zodra de template werd toegepast.
    -->

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Typography.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="ComboBoxItem">
        <Setter Property="Padding" Value="{StaticResource SpaceSmall}" />
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="Bd" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <!-- Foreground mee-switchen: het item wordt hier
                             AccentBrush-gekleurd, en TextPrimaryBrush kan
                             in een thema (LCARS) toevallig gelijk zijn aan
                             AccentBrush - zonder deze regel oranje-op-oranje. -->
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentBrush}" />
                            <Setter Property="Foreground" Value="{DynamicResource AccentForegroundBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBox">
        <!-- Background is AccentBrush (zie de class-comment hierboven),
             dus Foreground moet AccentForegroundBrush zijn - TextPrimaryBrush
             kan in een thema (LCARS) toevallig gelijk zijn aan AccentBrush,
             wat oranje-op-oranje tekst zou geven. -->
        <Setter Property="Background" Value="{DynamicResource AccentBrush}" />
        <Setter Property="Foreground" Value="{DynamicResource AccentForegroundBrush}" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="FontFamily" Value="{StaticResource AppFontFamily}" />
        <Setter Property="FontSize" Value="{StaticResource FontSizeSmall}" />
        <Setter Property="Padding" Value="{StaticResource SpaceSmall}" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton x:Name="ToggleButton"
                                      Background="{TemplateBinding Background}"
                                      IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                      ClickMode="Press"
                                      Focusable="False">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border Background="{TemplateBinding Background}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="20" />
                                            </Grid.ColumnDefinitions>
                                            <Path Grid.Column="1"
                                                  Data="M0,0 L4,4 L8,0 Z"
                                                  Fill="{DynamicResource AccentForegroundBrush}"
                                                  HorizontalAlignment="Center"
                                                  VerticalAlignment="Center" />
                                        </Grid>
                                    </Border>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <ContentPresenter x:Name="ContentSite"
                                          IsHitTestVisible="False"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                          Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="Left"
                                          VerticalAlignment="Center" />
                        <Popup x:Name="Popup"
                               Placement="Bottom"
                               IsOpen="{TemplateBinding IsDropDownOpen}"
                               AllowsTransparency="True"
                               Focusable="False"
                               PopupAnimation="Slide">
                            <Border Background="{DynamicResource SurfaceBrush}"
                                    BorderBrush="{DynamicResource BorderBrush}"
                                    BorderThickness="1"
                                    MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                                    MaxHeight="200">
                                <ScrollViewer>
                                    <ItemsPresenter />
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
