<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        Visual Studio (Dark+) thema - de vertrouwde donkere IDE-look
        (editor-achtergrond #1E1E1E, VS-blauw #007ACC als accent). Zelfde
        sleutel-set als Lcars.xaml, zie de uitleg daar.
    -->

    <Color x:Key="AccentColor">#007ACC</Color>
    <Color x:Key="AccentColorDark">#005A9E</Color>
    <Color x:Key="BackgroundColor">#1E1E1E</Color>
    <Color x:Key="SurfaceColor">#252526</Color>
    <Color x:Key="BorderColor">#3F3F46</Color>
    <Color x:Key="TextPrimaryColor">#D4D4D4</Color>
    <Color x:Key="TextMutedColor">#9D9D9D</Color>
    <!-- Tekst/iconen bovenop een AccentBrush-achtergrond (knoppen, actieve
         tab) - zelfde token als Lcars.xaml, zie de uitleg daar. VS-blauw
         is donker genoeg dat wit hier het best contrasteert. -->
    <Color x:Key="AccentForegroundColor">#FFFFFF</Color>

    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}" />
    <SolidColorBrush x:Key="AccentDarkBrush" Color="{StaticResource AccentColorDark}" />
    <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}" />
    <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}" />
    <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}" />
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}" />
    <SolidColorBrush x:Key="TextMutedBrush" Color="{StaticResource TextMutedColor}" />
    <SolidColorBrush x:Key="AccentForegroundBrush" Color="{StaticResource AccentForegroundColor}" />

</ResourceDictionary>
