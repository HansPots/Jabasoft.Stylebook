<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!--
        Donker thema (standaard). Levert dezelfde sleutel-set als
        Light.xaml in deze map - Theming/ThemeManager.cs verwisselt op
        naam, dus elk thema-bestand moet exact dezelfde x:Key's hebben.
        Componenten refereren nooit rechtstreeks naar Dark/Light, alleen
        naar de gedeelde namen (AccentBrush, SurfaceBrush, ...) via
        {DynamicResource ...} - dat is wat het live wisselen mogelijk
        maakt.
    -->

    <Color x:Key="AccentColor">#4A7FE8</Color>
    <Color x:Key="AccentColorDark">#2F5FC7</Color>
    <Color x:Key="BackgroundColor">#1B1B1F</Color>
    <Color x:Key="SurfaceColor">#26262B</Color>
    <Color x:Key="BorderColor">#3A3A40</Color>
    <Color x:Key="TextPrimaryColor">#F2F2F2</Color>
    <Color x:Key="TextMutedColor">#A0A0A8</Color>

    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}" />
    <SolidColorBrush x:Key="AccentDarkBrush" Color="{StaticResource AccentColorDark}" />
    <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}" />
    <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}" />
    <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}" />
    <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}" />
    <SolidColorBrush x:Key="TextMutedBrush" Color="{StaticResource TextMutedColor}" />

</ResourceDictionary>
