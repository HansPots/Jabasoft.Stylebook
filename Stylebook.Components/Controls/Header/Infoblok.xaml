<UserControl x:Class="Stylebook.Components.Controls.Header.Infoblok"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">

    <!--
        Header-regio component: een simpel informatieblokje (titel + body-
        tekst) om in de header te plaatsen. Kleuren via DynamicResource,
        volgt dus het actieve thema. Te bekijken via Stylebook.Playground's
        Bibliotheek-modus.
    -->
    <Border Background="{DynamicResource SurfaceBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="6"
            Padding="16"
            Width="280">
        <StackPanel>
            <TextBlock Text="InfoBlok" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,8" TextWrapping="Wrap" />
            <TextBlock Text="Dit is een informatieblok voor de header." Foreground="{DynamicResource TextMutedBrush}" TextWrapping="Wrap" />
        </StackPanel>
    </Border>

</UserControl>
