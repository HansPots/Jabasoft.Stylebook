<UserControl x:Class="Stylebook.Components.Regions.Algemeen.JabaSoftWordmark"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">

    <!-- Algemeen: het "JabaSoft the Application"-beeldmerk. -->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.RowSpan="2" Grid.Column="0"
                   Text="Jaba"
                   FontWeight="Bold"
                   FontSize="48"
                   Foreground="{DynamicResource AccentBrush}"
                   VerticalAlignment="Bottom"/>

        <TextBlock Grid.Row="0" Grid.Column="1"
                   Text="Soft"
                   FontWeight="Bold"
                   FontSize="36"
                   Foreground="{DynamicResource Text01Brush}"
                   VerticalAlignment="Bottom"
                   Margin="0,0,0,-8"/>

        <TextBlock Grid.Row="1" Grid.Column="1"
                   Text="the Application"
                   FontSize="12"
                   Foreground="{DynamicResource TextMutedBrush}"
                   HorizontalAlignment="Right"
                   VerticalAlignment="Bottom"
                   Margin="0,0,0,8"/>
    </Grid>

</UserControl>
