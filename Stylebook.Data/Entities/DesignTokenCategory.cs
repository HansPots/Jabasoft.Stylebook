namespace Stylebook.Data.Entities;

/// <summary>How a DesignToken's Value string should be parsed and what WPF type it becomes.</summary>
public enum DesignTokenCategory
{
    Color,
    Radius,
    Spacing,
    FontSize,
    FontFamily,
}
