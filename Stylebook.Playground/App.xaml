<Application x:Class="Stylebook.Playground.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Stylebook.Playground"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Shared typography/styles every real app merges in too
                     - see Stylebook.Components/Styles/Theme.xaml. -->
                <ResourceDictionary Source="pack://application:,,,/Stylebook.Components;component/Styles/Theme.xaml" />
                <!-- Starting color theme - kept as its own top-level entry
                     (not nested inside Theme.xaml) so Theming/ThemeManager
                     can swap it live, see that class for why. -->
                <ResourceDictionary Source="pack://application:,,,/Stylebook.Components;component/Styles/Themes/Dark.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Application.Resources>
</Application>
