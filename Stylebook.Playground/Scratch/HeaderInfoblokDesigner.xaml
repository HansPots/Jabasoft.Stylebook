<UserControl x:Class="Stylebook.Playground.Scratch.HeaderInfoblokDesigner"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="#FF1E1E1E">

    <!--
        Scratch-kopie van het "Infoblok"-component (Stylebook-database,
        Header-regio) - zie ComponentDesigner.xaml voor het algemene
        gebruik van dit soort bestanden. Dit exemplaar begint NIET leeg:
        de XAML hieronder is exact wat er nu in de database staat.

        Let op: dit is bewust GEEN onderdeel van de draaiende app (niets
        verwijst hiernaar) - puur een ontwerp-hulpmiddel. Een aanpassing
        hier komt pas in Stylebook/Jabasoft terecht als de resulterende
        XAML teruggezet wordt in de database (component "Infoblok").
    -->
    <Border xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            Background="{DynamicResource SurfaceBrush}"
            BorderBrush="{DynamicResource BorderBrush}"
            BorderThickness="1"
            CornerRadius="6"
            Padding="16"
            Width="280">
        <StackPanel>
            <TextBlock Text="InfoBlok" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,8" TextWrapping="Wrap" />
            <TextBlock Text="Dit is een informatieblok voor de header." Foreground="{DynamicResource TextMutedBrush}" TextWrapping="Wrap" />
        </StackPanel>
    </Border>

</UserControl>
