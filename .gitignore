bin/
obj/
.vs/
*.user

# Disposable page snapshots regenerated by Stylebook's "Pagina's
# verversen" (see Program.cs) - cleared on every startup, never meant
# to be committed.
Stylebook.Web/wwwroot/dummy-pages/

# Runtime-writable settings file (see Program.cs's ReadAiConnectorSettingsAsync)
Stylebook.Web/ai-connector.json
